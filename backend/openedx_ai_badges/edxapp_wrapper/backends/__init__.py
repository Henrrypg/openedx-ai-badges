"""
Concrete backend implementations for the edxapp_wrapper modules.

Each backend module targets a specific Open edX release and imports the
platform symbols directly (e.g. xmodule, cms.djangoapps).
"""
